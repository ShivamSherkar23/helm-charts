apiVersion: v1
kind: Service
metadata:
  name: {{ include "quill-webapp.fullname" . }}
  labels:
    {{- include "quill-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quill-webapp.selectorLabels" . | nindent 4 }}
